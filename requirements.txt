# Runtime deps for the pKa predictor (installed in the Docker image).
# torch comes from the base image (pytorch/pytorch:2.8.0-cpu or -cuda); listed
# here for completeness / non-Docker installs.
torch>=2.1
fairchem-core>=1.4
rdkit>=2023.3
scikit-learn>=1.3
xgboost>=2.0
gradio>=4.0
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
ase>=3.22
joblib>=1.3
